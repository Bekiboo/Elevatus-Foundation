// import { redirect } from '@sveltejs/kit'
// import { checkIfAdmin } from '$lib/db/auth';

// export const load = async ({ locals }) => {
//   // redirect user if not logged in
//   checkIfAdmin()
//   console.log('locals.user: ' + JSON.stringify(locals.user));
//   if (!locals.user) {
//     throw redirect(302, '/')
//   }
// }
